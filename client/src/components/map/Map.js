import React, { Component } from 'react';
import MapBox from '../../containers/map/MapBox';

export default class Map extends Component {
    render() {
        return (
            <MapBox />
        )
    }
}